# API Base URL
BASE_URL=https://api.hh.ru

# Request timeout in seconds
TIMEOUT=30

# Auth token for tests (optional)
TEST_TOKEN=

# Retry settings
RETRY_COUNT=2
RETRY_DELAY=0.3

# Throttle settings (ignored in CI)
MIN_REQUEST_INTERVAL=0.1

# Logging level: DEBUG, INFO, WARNING
LOG_LEVEL=INFO
